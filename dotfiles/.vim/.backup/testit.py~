#!/bin/python

import speech_recognition as sr

r = sr.Recognizer()

filepath = 'test.wav'

with sr.AudioFile(filepath) as source:
    print(source)
    text = r.listen(source)
    print(text)

try:
    plaintext = r.recognize_google(text)
    print(text)
except Exception as e:
    print(e)
