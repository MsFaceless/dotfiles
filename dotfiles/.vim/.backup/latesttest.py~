#!/bin/python

import requests
import json
from datetime import datetime

target = "https://gateway.calabashpay.co.za"
pub_target = f"{target}/notification/create_eiffel_corp_premium_ad_payment_notification"

def post_to_campus_user(entity_id=1):
    username = 'NickyHill'
    data = {
        'entity_id':6,
        'message':'Latest Message for Tafadzwa',
        'amount':1000,
    }
    r = requests.post(pub_target, json=data)
    return r

if __name__ == '__main__':
    response = post_to_campus_user()
    print(response.text)
