import requests

baseurl = "http://localhost:8000/calabash"

data = {
    'username': 'calabash',
    'password': 'test1234',
    'email': 'sean@testmail.com',
    'mobile': '0746379920',
    'firstName': 'Sean',
    'surname': 'Grey',
    'sendMail': False,
}

clone_data = {
    'username': 'cloneuser',
    'password': 'cloneuser',
    'email': 'sean@testmail.com',
    'mobile': '0746379923',
    'firstName': 'clone',
    'surname': 'cloneson',
    'sendMail': False,
}

org_data = {
    'username': 'calabash',
    'password': 'test1234',
    'email': 'sean@testmail.com',
    'mobile': '0756379920',
    'firstName': 'Sean',
    'surname': 'Grey',
    'sendMail': False,
}
#r = requests.post(f"{baseurl}/create_myimali_person?", json=data)
#r = requests.post(f"{baseurl}/create_myimali_person?", json=clone_data)
#r = requests.post(f"{baseurl}/create_myimali_organisation?", json=org_data)
#r = requests.get(f"{baseurl}/get_entity_object_by_id?", params={'target_id':1})
#r = requests.get(f"{baseurl}/get_entity_object_by_id?", params={'target_id':2})
#print(r.json())
r = requests.get(f"{baseurl}/get_entity_by_mobile?", params={'mobile_number':'0746379920'})
print(r.json())
r = requests.get(f"{baseurl}/get_entity_by_mobile?", params={'mobile_number':'0746379923'})
print(r.json())
r = requests.get(f"{baseurl}/get_entity_by_mobile?", params={'mobile_number':'0756379920'})
print(r.json())
