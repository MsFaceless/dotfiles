#!/bin/python

import os
import pandas as pd

filepath = os.path.join(os.getcwd() + '/output')

for root, dirs, files in os.walk(filepath):
    for each in files:
        print(each)

        fullpath = os.path.join(root, each)
        openfile = pd.read_csv(fullpath)

        print(openfile)

        break
