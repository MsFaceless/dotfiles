<html py:extends="master.xhtml" py:strip="True">
<head py:block="head" py:strip="True">
    <title py:block="master_title">${getattr(tmpl_context, 'project_app_name', 'TurboGears2')}</title>
</head>

<body py:block="navigated_content" py:strip="True">
<div class="t-content flex-fill">
    <!-- Navigation Bar -->
    <div class="t-navigation">
        <nav class="navbar navbar-expand nav-pills">
            <ul class="navbar-nav">

                <py:for each="(root, link, active) in navigation_bar">
                    ${root} ${link} ${active}
                </py:for>

            </ul>
        </nav>
    </div>
    <!-- Navigation Tabs-->
    <ul class="nav nav-tabs nav-fill">
        <py:def function="navigation_tabs_gui(title, active)">
            <li class="nav-item">
                <a class="nav-link ${active}" href="#">${title}</a>
            </li>
        </py:def>
        <py:for each="title, active in navigation_tabs">
            ${navigation_tabs_gui(title, active)}
        </py:for>
    </ul>
</div>
</body>
</html>
